/**
 * Fatture in Cloud API v2 - API Reference
 * Connect your software with Fatture in Cloud, the invoicing platform chosen by more than 500.000 businesses in Italy.   The Fatture in Cloud API is based on REST, and makes possible to interact with the user related data prior authorization via OAuth2 protocol.
 *
 * The version of the OpenAPI document: 2.1.5
 * Contact: info@fattureincloud.it
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient'
import PaymentAccount from './PaymentAccount'
import PaymentMethod from './PaymentMethod'
import PriceList from './PriceList'
import VatType from './VatType'

/**
 * The EntityClientPreCreateInfo model module.
 * @module model/EntityClientPreCreateInfo
 * @version 2.1.2
 */
class EntityClientPreCreateInfo {
  /**
     * Constructs a new <code>EntityClientPreCreateInfo</code>.
     * @alias module:model/EntityClientPreCreateInfo
     */
  constructor () {
    EntityClientPreCreateInfo.initialize(this)
  }

  /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
  static initialize (obj) {
  }

  /**
     * Constructs a <code>EntityClientPreCreateInfo</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EntityClientPreCreateInfo} obj Optional instance to populate.
     * @return {module:model/EntityClientPreCreateInfo} The populated <code>EntityClientPreCreateInfo</code> instance.
     */
  static constructFromObject (data, obj) {
    if (data) {
      obj = obj || new EntityClientPreCreateInfo()

      if (data.hasOwnProperty('countries_list')) {
        obj.countries_list = ApiClient.convertToType(data.countries_list, ['String'])
      }
      if (data.hasOwnProperty('payment_methods_list')) {
        obj.payment_methods_list = ApiClient.convertToType(data.payment_methods_list, [PaymentMethod])
      }
      if (data.hasOwnProperty('payment_accounts_list')) {
        obj.payment_accounts_list = ApiClient.convertToType(data.payment_accounts_list, [PaymentAccount])
      }
      if (data.hasOwnProperty('vat_types_list')) {
        obj.vat_types_list = ApiClient.convertToType(data.vat_types_list, [VatType])
      }
      if (data.hasOwnProperty('price_lists')) {
        obj.price_lists = ApiClient.convertToType(data.price_lists, [PriceList])
      }
      if (data.hasOwnProperty('limit')) {
        obj.limit = ApiClient.convertToType(data.limit, 'Number')
      }
      if (data.hasOwnProperty('usage')) {
        obj.usage = ApiClient.convertToType(data.usage, 'Number')
      }
    }
    return obj
  }

  /**
     * Validates the JSON data with respect to <code>EntityClientPreCreateInfo</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EntityClientPreCreateInfo</code>.
     */
  static validateJSON (data) {
    // ensure the json data is an array
    if (!Array.isArray(data.countries_list)) {
      throw new Error('Expected the field `countries_list` to be an array in the JSON data but got ' + data.countries_list)
    }
    if (data.payment_methods_list) { // data not null
      // ensure the json data is an array
      if (!Array.isArray(data.payment_methods_list)) {
        throw new Error('Expected the field `payment_methods_list` to be an array in the JSON data but got ' + data.payment_methods_list)
      }
      // validate the optional field `payment_methods_list` (array)
      for (const item of data.payment_methods_list) {
        PaymentMethod.validateJSON(item)
      }
    }
    if (data.payment_accounts_list) { // data not null
      // ensure the json data is an array
      if (!Array.isArray(data.payment_accounts_list)) {
        throw new Error('Expected the field `payment_accounts_list` to be an array in the JSON data but got ' + data.payment_accounts_list)
      }
      // validate the optional field `payment_accounts_list` (array)
      for (const item of data.payment_accounts_list) {
        PaymentAccount.validateJSON(item)
      }
    }
    if (data.vat_types_list) { // data not null
      // ensure the json data is an array
      if (!Array.isArray(data.vat_types_list)) {
        throw new Error('Expected the field `vat_types_list` to be an array in the JSON data but got ' + data.vat_types_list)
      }
      // validate the optional field `vat_types_list` (array)
      for (const item of data.vat_types_list) {
        VatType.validateJSON(item)
      }
    }
    if (data.price_lists) { // data not null
      // ensure the json data is an array
      if (!Array.isArray(data.price_lists)) {
        throw new Error('Expected the field `price_lists` to be an array in the JSON data but got ' + data.price_lists)
      }
      // validate the optional field `price_lists` (array)
      for (const item of data.price_lists) {
        PriceList.validateJSON(item)
      }
    }

    return true
  }
}

/**
 * Countries list
 * @member {Array.<String>} countries_list
 */
EntityClientPreCreateInfo.prototype.countries_list = undefined

/**
 * Entity payment methods list
 * @member {Array.<module:model/PaymentMethod>} payment_methods_list
 */
EntityClientPreCreateInfo.prototype.payment_methods_list = undefined

/**
 * Entity payment accounts list
 * @member {Array.<module:model/PaymentAccount>} payment_accounts_list
 */
EntityClientPreCreateInfo.prototype.payment_accounts_list = undefined

/**
 * Vat types list
 * @member {Array.<module:model/VatType>} vat_types_list
 */
EntityClientPreCreateInfo.prototype.vat_types_list = undefined

/**
 * Entity price lists
 * @member {Array.<module:model/PriceList>} price_lists
 */
EntityClientPreCreateInfo.prototype.price_lists = undefined

/**
 * Entity limit
 * @member {Number} limit
 */
EntityClientPreCreateInfo.prototype.limit = undefined

/**
 * Entity usage
 * @member {Number} usage
 */
EntityClientPreCreateInfo.prototype.usage = undefined

export default EntityClientPreCreateInfo
