/**
 * Fatture in Cloud API v2 - API Reference
 * Connect your software with Fatture in Cloud, the invoicing platform chosen by more than 500.000 businesses in Italy.   The Fatture in Cloud API is based on REST, and makes possible to interact with the user related data prior authorization via OAuth2 protocol.
 *
 * The version of the OpenAPI document: 2.1.8
 * Contact: info@fattureincloud.it
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient'
import TemplateType from './TemplateType'

/**
 * The DocumentTemplate model module.
 * @module model/DocumentTemplate
 * @version 2.1.3
 */
class DocumentTemplate {
  /**
     * Constructs a new <code>DocumentTemplate</code>.
     * @alias module:model/DocumentTemplate
     */
  constructor () {
    DocumentTemplate.initialize(this)
  }

  /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
  static initialize (obj) {
  }

  /**
     * Constructs a <code>DocumentTemplate</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DocumentTemplate} obj Optional instance to populate.
     * @return {module:model/DocumentTemplate} The populated <code>DocumentTemplate</code> instance.
     */
  static constructFromObject (data, obj) {
    if (data) {
      obj = obj || new DocumentTemplate()

      if (data.hasOwnProperty('id')) {
        obj.id = ApiClient.convertToType(data.id, 'Number')
      }
      if (data.hasOwnProperty('privacy')) {
        obj.privacy = ApiClient.convertToType(data.privacy, 'String')
      }
      if (data.hasOwnProperty('type')) {
        obj.type = TemplateType.constructFromObject(data.type)
      }
      if (data.hasOwnProperty('name')) {
        obj.name = ApiClient.convertToType(data.name, 'String')
      }
      if (data.hasOwnProperty('can_disable_watermark')) {
        obj.can_disable_watermark = ApiClient.convertToType(data.can_disable_watermark, 'Boolean')
      }
      if (data.hasOwnProperty('author')) {
        obj.author = ApiClient.convertToType(data.author, 'String')
      }
      if (data.hasOwnProperty('content')) {
        obj.content = ApiClient.convertToType(data.content, 'String')
      }
      if (data.hasOwnProperty('supports_custom_taxable')) {
        obj.supports_custom_taxable = ApiClient.convertToType(data.supports_custom_taxable, 'Boolean')
      }
    }
    return obj
  }

  /**
     * Validates the JSON data with respect to <code>DocumentTemplate</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DocumentTemplate</code>.
     */
  static validateJSON (data) {
    // ensure the json data is a string
    if (data.privacy && !(typeof data.privacy === 'string' || data.privacy instanceof String)) {
      throw new Error('Expected the field `privacy` to be a primitive type in the JSON string but got ' + data.privacy)
    }
    // ensure the json data is a string
    if (data.type && !(typeof data.type === 'string' || data.type instanceof String)) {
      throw new Error('Expected the field `type` to be a primitive type in the JSON string but got ' + data.type)
    }
    // ensure the json data is a string
    if (data.name && !(typeof data.name === 'string' || data.name instanceof String)) {
      throw new Error('Expected the field `name` to be a primitive type in the JSON string but got ' + data.name)
    }
    // ensure the json data is a string
    if (data.author && !(typeof data.author === 'string' || data.author instanceof String)) {
      throw new Error('Expected the field `author` to be a primitive type in the JSON string but got ' + data.author)
    }
    // ensure the json data is a string
    if (data.content && !(typeof data.content === 'string' || data.content instanceof String)) {
      throw new Error('Expected the field `content` to be a primitive type in the JSON string but got ' + data.content)
    }

    return true
  }
}

/**
 * Template id
 * @member {Number} id
 */
DocumentTemplate.prototype.id = undefined

/**
 * Template privacy
 * @member {String} privacy
 */
DocumentTemplate.prototype.privacy = undefined

/**
 * @member {module:model/TemplateType} type
 */
DocumentTemplate.prototype.type = undefined

/**
 * Template name
 * @member {String} name
 */
DocumentTemplate.prototype.name = undefined

/**
 * Can disable watermark
 * @member {Boolean} can_disable_watermark
 */
DocumentTemplate.prototype.can_disable_watermark = undefined

/**
 * Template author
 * @member {String} author
 */
DocumentTemplate.prototype.author = undefined

/**
 * Template definition content
 * @member {String} content
 */
DocumentTemplate.prototype.content = undefined

/**
 * Supports custom taxable
 * @member {Boolean} supports_custom_taxable
 */
DocumentTemplate.prototype.supports_custom_taxable = undefined

export default DocumentTemplate
