/**
 * Fatture in Cloud API v2 - API Reference
 * Connect your software with Fatture in Cloud, the invoicing platform chosen by more than 500.000 businesses in Italy.   The Fatture in Cloud API is based on REST, and makes possible to interact with the user related data prior authorization via OAuth2 protocol.
 *
 * The version of the OpenAPI document: 2.1.5
 * Contact: info@fattureincloud.it
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient'
import PriceListPricesType from './PriceListPricesType'
import PriceListType from './PriceListType'

/**
 * The PriceList model module.
 * @module model/PriceList
 * @version 2.1.2
 */
class PriceList {
  /**
     * Constructs a new <code>PriceList</code>.
     * @alias module:model/PriceList
     */
  constructor () {
    PriceList.initialize(this)
  }

  /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
  static initialize (obj) {
  }

  /**
     * Constructs a <code>PriceList</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PriceList} obj Optional instance to populate.
     * @return {module:model/PriceList} The populated <code>PriceList</code> instance.
     */
  static constructFromObject (data, obj) {
    if (data) {
      obj = obj || new PriceList()

      if (data.hasOwnProperty('id')) {
        obj.id = ApiClient.convertToType(data.id, 'String')
      }
      if (data.hasOwnProperty('name')) {
        obj.name = ApiClient.convertToType(data.name, 'String')
      }
      if (data.hasOwnProperty('prices_type')) {
        obj.prices_type = PriceListPricesType.constructFromObject(data.prices_type)
      }
      if (data.hasOwnProperty('is_default')) {
        obj.is_default = ApiClient.convertToType(data.is_default, 'Boolean')
      }
      if (data.hasOwnProperty('valid_from')) {
        obj.valid_from = ApiClient.convertToType(data.valid_from, 'String')
      }
      if (data.hasOwnProperty('valid_to')) {
        obj.valid_to = ApiClient.convertToType(data.valid_to, 'String')
      }
      if (data.hasOwnProperty('type')) {
        obj.type = PriceListType.constructFromObject(data.type)
      }
    }
    return obj
  }

  /**
     * Validates the JSON data with respect to <code>PriceList</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PriceList</code>.
     */
  static validateJSON (data) {
    // ensure the json data is a string
    if (data.id && !(typeof data.id === 'string' || data.id instanceof String)) {
      throw new Error('Expected the field `id` to be a primitive type in the JSON string but got ' + data.id)
    }
    // ensure the json data is a string
    if (data.name && !(typeof data.name === 'string' || data.name instanceof String)) {
      throw new Error('Expected the field `name` to be a primitive type in the JSON string but got ' + data.name)
    }
    // ensure the json data is a string
    if (data.valid_from && !(typeof data.valid_from === 'string' || data.valid_from instanceof String)) {
      throw new Error('Expected the field `valid_from` to be a primitive type in the JSON string but got ' + data.valid_from)
    }
    // ensure the json data is a string
    if (data.valid_to && !(typeof data.valid_to === 'string' || data.valid_to instanceof String)) {
      throw new Error('Expected the field `valid_to` to be a primitive type in the JSON string but got ' + data.valid_to)
    }

    return true
  }
}

/**
 * Price list id
 * @member {String} id
 */
PriceList.prototype.id = undefined

/**
 * Price list name
 * @member {String} name
 */
PriceList.prototype.name = undefined

/**
 * @member {module:model/PriceListPricesType} prices_type
 */
PriceList.prototype.prices_type = undefined

/**
 * This entity is default
 * @member {Boolean} is_default
 */
PriceList.prototype.is_default = undefined

/**
 * Price list validity start date
 * @member {String} valid_from
 */
PriceList.prototype.valid_from = undefined

/**
 * Price list validity end date
 * @member {String} valid_to
 */
PriceList.prototype.valid_to = undefined

/**
 * @member {module:model/PriceListType} type
 */
PriceList.prototype.type = undefined

export default PriceList
