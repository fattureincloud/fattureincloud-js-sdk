/**
 * Fatture in Cloud API v2 - API Reference
 * Connect your software with Fatture in Cloud, the invoicing platform chosen by more than 500.000 businesses in Italy.   The Fatture in Cloud API is based on REST, and makes possible to interact with the user related data prior authorization via OAuth2 protocol.
 *
 * The version of the OpenAPI document: 2.1.8
 * Contact: info@fattureincloud.it
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient'
import Attachment from './Attachment'
import Currency from './Currency'
import PendingReceivedDocumentExtractedData from './PendingReceivedDocumentExtractedData'
import PendingReceivedDocumentPaymentsListItem from './PendingReceivedDocumentPaymentsListItem'
import PendingReceivedDocumentType from './PendingReceivedDocumentType'
import ReceivedDocumentType from './ReceivedDocumentType'

/**
 * The PendingReceivedDocument model module.
 * @module model/PendingReceivedDocument
 * @version 2.1.3
 */
class PendingReceivedDocument {
  /**
     * Constructs a new <code>PendingReceivedDocument</code>.
     * @alias module:model/PendingReceivedDocument
     */
  constructor () {
    PendingReceivedDocument.initialize(this)
  }

  /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
  static initialize (obj) {
  }

  /**
     * Constructs a <code>PendingReceivedDocument</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PendingReceivedDocument} obj Optional instance to populate.
     * @return {module:model/PendingReceivedDocument} The populated <code>PendingReceivedDocument</code> instance.
     */
  static constructFromObject (data, obj) {
    if (data) {
      obj = obj || new PendingReceivedDocument()

      if (data.hasOwnProperty('id')) {
        obj.id = ApiClient.convertToType(data.id, 'Number')
      }
      if (data.hasOwnProperty('date')) {
        obj.date = ApiClient.convertToType(data.date, 'Date')
      }
      if (data.hasOwnProperty('subject')) {
        obj.subject = ApiClient.convertToType(data.subject, 'String')
      }
      if (data.hasOwnProperty('filename')) {
        obj.filename = ApiClient.convertToType(data.filename, 'String')
      }
      if (data.hasOwnProperty('type')) {
        obj.type = PendingReceivedDocumentType.constructFromObject(data.type)
      }
      if (data.hasOwnProperty('attachment_url')) {
        obj.attachment_url = ApiClient.convertToType(data.attachment_url, 'String')
      }
      if (data.hasOwnProperty('amount_gross')) {
        obj.amount_gross = ApiClient.convertToType(data.amount_gross, 'Number')
      }
      if (data.hasOwnProperty('currency')) {
        obj.currency = Currency.constructFromObject(data.currency)
      }
      if (data.hasOwnProperty('document_type')) {
        obj.document_type = ReceivedDocumentType.constructFromObject(data.document_type)
      }
      if (data.hasOwnProperty('supplier_name')) {
        obj.supplier_name = ApiClient.convertToType(data.supplier_name, 'String')
      }
      if (data.hasOwnProperty('cost_center')) {
        obj.cost_center = ApiClient.convertToType(data.cost_center, 'String')
      }
      if (data.hasOwnProperty('category')) {
        obj.category = ApiClient.convertToType(data.category, 'String')
      }
      if (data.hasOwnProperty('other_attachments')) {
        obj.other_attachments = ApiClient.convertToType(data.other_attachments, [Attachment])
      }
      if (data.hasOwnProperty('emssion_date')) {
        obj.emssion_date = ApiClient.convertToType(data.emssion_date, 'Date')
      }
      if (data.hasOwnProperty('payments_list')) {
        obj.payments_list = ApiClient.convertToType(data.payments_list, [PendingReceivedDocumentPaymentsListItem])
      }
      if (data.hasOwnProperty('amount_net')) {
        obj.amount_net = ApiClient.convertToType(data.amount_net, 'Number')
      }
      if (data.hasOwnProperty('amount_vat')) {
        obj.amount_vat = ApiClient.convertToType(data.amount_vat, 'Number')
      }
      if (data.hasOwnProperty('import_error')) {
        obj.import_error = ApiClient.convertToType(data.import_error, 'String')
      }
      if (data.hasOwnProperty('extracted_data')) {
        obj.extracted_data = PendingReceivedDocumentExtractedData.constructFromObject(data.extracted_data)
      }
    }
    return obj
  }

  /**
     * Validates the JSON data with respect to <code>PendingReceivedDocument</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PendingReceivedDocument</code>.
     */
  static validateJSON (data) {
    // ensure the json data is a string
    if (data.subject && !(typeof data.subject === 'string' || data.subject instanceof String)) {
      throw new Error('Expected the field `subject` to be a primitive type in the JSON string but got ' + data.subject)
    }
    // ensure the json data is a string
    if (data.filename && !(typeof data.filename === 'string' || data.filename instanceof String)) {
      throw new Error('Expected the field `filename` to be a primitive type in the JSON string but got ' + data.filename)
    }
    // ensure the json data is a string
    if (data.attachment_url && !(typeof data.attachment_url === 'string' || data.attachment_url instanceof String)) {
      throw new Error('Expected the field `attachment_url` to be a primitive type in the JSON string but got ' + data.attachment_url)
    }
    // validate the optional field `currency`
    if (data.currency) { // data not null
      Currency.validateJSON(data.currency)
    }
    // ensure the json data is a string
    if (data.supplier_name && !(typeof data.supplier_name === 'string' || data.supplier_name instanceof String)) {
      throw new Error('Expected the field `supplier_name` to be a primitive type in the JSON string but got ' + data.supplier_name)
    }
    // ensure the json data is a string
    if (data.cost_center && !(typeof data.cost_center === 'string' || data.cost_center instanceof String)) {
      throw new Error('Expected the field `cost_center` to be a primitive type in the JSON string but got ' + data.cost_center)
    }
    // ensure the json data is a string
    if (data.category && !(typeof data.category === 'string' || data.category instanceof String)) {
      throw new Error('Expected the field `category` to be a primitive type in the JSON string but got ' + data.category)
    }
    if (data.other_attachments) { // data not null
      // ensure the json data is an array
      if (!Array.isArray(data.other_attachments)) {
        throw new Error('Expected the field `other_attachments` to be an array in the JSON data but got ' + data.other_attachments)
      }
      // validate the optional field `other_attachments` (array)
      for (const item of data.other_attachments) {
        Attachment.validateJSON(item)
      }
    }
    if (data.payments_list) { // data not null
      // ensure the json data is an array
      if (!Array.isArray(data.payments_list)) {
        throw new Error('Expected the field `payments_list` to be an array in the JSON data but got ' + data.payments_list)
      }
      // validate the optional field `payments_list` (array)
      for (const item of data.payments_list) {
        PendingReceivedDocumentPaymentsListItem.validateJSON(item)
      }
    }
    // ensure the json data is a string
    if (data.import_error && !(typeof data.import_error === 'string' || data.import_error instanceof String)) {
      throw new Error('Expected the field `import_error` to be a primitive type in the JSON string but got ' + data.import_error)
    }
    // validate the optional field `extracted_data`
    if (data.extracted_data) { // data not null
      PendingReceivedDocumentExtractedData.validateJSON(data.extracted_data)
    }

    return true
  }
}

/**
 * Pending received document id
 * @member {Number} id
 */
PendingReceivedDocument.prototype.id = undefined

/**
 * Pending received document date
 * @member {Date} date
 */
PendingReceivedDocument.prototype.date = undefined

/**
 * Pending received document subject
 * @member {String} subject
 */
PendingReceivedDocument.prototype.subject = undefined

/**
 * Pending received document filename
 * @member {String} filename
 */
PendingReceivedDocument.prototype.filename = undefined

/**
 * @member {module:model/PendingReceivedDocumentType} type
 */
PendingReceivedDocument.prototype.type = undefined

/**
 * [Temporary] [Read Only] Pending received document url of the attached file
 * @member {String} attachment_url
 */
PendingReceivedDocument.prototype.attachment_url = undefined

/**
 * [Read Only] Pending received document total gross amount
 * @member {Number} amount_gross
 */
PendingReceivedDocument.prototype.amount_gross = undefined

/**
 * @member {module:model/Currency} currency
 */
PendingReceivedDocument.prototype.currency = undefined

/**
 * @member {module:model/ReceivedDocumentType} document_type
 */
PendingReceivedDocument.prototype.document_type = undefined

/**
 * Pending received document supplier name
 * @member {String} supplier_name
 */
PendingReceivedDocument.prototype.supplier_name = undefined

/**
 * Pending received document cost center
 * @member {String} cost_center
 */
PendingReceivedDocument.prototype.cost_center = undefined

/**
 * Pending received document category
 * @member {String} category
 */
PendingReceivedDocument.prototype.category = undefined

/**
 * Pending received document other attachments
 * @member {Array.<module:model/Attachment>} other_attachments
 */
PendingReceivedDocument.prototype.other_attachments = undefined

/**
 * Pending received document emission date
 * @member {Date} emssion_date
 */
PendingReceivedDocument.prototype.emssion_date = undefined

/**
 * @member {Array.<module:model/PendingReceivedDocumentPaymentsListItem>} payments_list
 */
PendingReceivedDocument.prototype.payments_list = undefined

/**
 * Pending received document total net amount
 * @member {Number} amount_net
 */
PendingReceivedDocument.prototype.amount_net = undefined

/**
 * Pending received document total vat amount
 * @member {Number} amount_vat
 */
PendingReceivedDocument.prototype.amount_vat = undefined

/**
 * Pending received document import error
 * @member {String} import_error
 */
PendingReceivedDocument.prototype.import_error = undefined

/**
 * @member {module:model/PendingReceivedDocumentExtractedData} extracted_data
 */
PendingReceivedDocument.prototype.extracted_data = undefined

export default PendingReceivedDocument
